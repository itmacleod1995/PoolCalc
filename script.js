function calc() {
    var chem = document.getElementById("chemNamewel").value;
    /**
     * Calculate Bicarbonate level
     */
    if (chem == "bicarb" || chem == "sodium bicarbonate") {
        console.log("Bicarb!");
    }
}